NAMESPACE osr.edw.staging.td.rms;

USING DataWarehouse.DataStore :: Structures.inboundQueueKey;
USING DataWarehouse.DataStore :: Structures.inboundQueueAttributes;
USING DataWarehouse.DataStore :: Structures.changeLogKey;
USING DataWarehouse.DataStore :: Structures.changeLogAttributes;
USING DataWarehouse.DataStore :: Structures.operationHistoryKey;
USING DataWarehouse.DataStore :: Structures.operationHistoryAttributes;
USING DataWarehouse.DataStore :: Structures.aggregationHistoryKey;
USING DataWarehouse.DataStore :: Structures.aggregationHistoryAttributes;
USING DataWarehouse.DataStore :: Structures.affectedRequestsKey;
USING DataWarehouse.DataStore :: Structures.logMessagesKey;
USING DataWarehouse.DataStore :: Structures.logMessagesAttributes;
USING DataWarehouse.DataStore :: Structures.subscribersKey;
USING DataWarehouse.DataStore :: Structures.subscribersAttributes;
USING DataWarehouse.DataStore :: Structures.runningOperationsKey;
USING DataWarehouse.DataStore :: Structures.idGeneratorKey;
USING DataWarehouse.DataStore :: Structures.idGeneratorAttributes;
USING DataWarehouse.DataStore :: Annotations AS DataStore;

CONTEXT CDS_TRM2
{

    /*@@layout{"layoutInfo":{"x":-239,"y":-79.5}}*/
    CONTEXT DSO
    {
        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1000DCL00000000
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT          : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID       : String(100)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID       : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID       : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    BUNDLE_ID       : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    VERSION         : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    FORM_NUMBER     : String(4)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    TIMESTAMP       : Decimal(15, 0) NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS   : String(800)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BP_NUMBER     : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_CO_FBN_NUMBER : String(12)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_CP_NAME       : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TELEPHONE     : String(30)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_DECL_CONFIRM  : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_DECL_DATE     : String(8)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_AUTO_APPROVED : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LAND_USAGE    : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_FORM_ANSWERS  : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_TMR_ADDRESS   : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_OPEN_TASKS    : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID    : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE   : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME   : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS    : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO    : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG     : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM    : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO      : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1000DCL00000000 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey        : inboundQueueKey;
                    technicalAttributes : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT              : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    BUNDLE_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    VERSION             : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    FORM_NUMBER         : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    TIMESTAMP           : Decimal(15, 0)         NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS       : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BP_NUMBER         : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_CO_FBN_NUMBER     : String(12)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_CP_NAME           : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TELEPHONE         : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_DECL_CONFIRM      : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_DECL_DATE         : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_AUTO_APPROVED     : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LAND_USAGE        : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_FORM_ANSWERS      : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_TMR_ADDRESS       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_OPEN_TASKS        : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME       : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS        : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM        : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO          : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1000DCL00000000 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey        : changeLogKey;
                    technicalAttributes : changeLogAttributes NOT NULL;
                KEY CLIENT              : String(3)           NOT NULL;
                KEY RECORD_ID           : String(100)         NOT NULL;
                    PARENT_ID           : String(32)          NOT NULL;
                    OBJECT_ID           : String(32)          NOT NULL;
                    BUNDLE_ID           : String(32)          NOT NULL;
                    VERSION             : String(5)           NOT NULL;
                    FORM_NUMBER         : String(4)           NOT NULL;
                    TIMESTAMP           : Decimal(15, 0)      NOT NULL;
                    INITIAL_FLAGS       : String(800)         NOT NULL;
                    A_BP_NUMBER         : String(10)          NOT NULL;
                    A_CO_FBN_NUMBER     : String(12)          NOT NULL;
                    A_CP_NAME           : String(40)          NOT NULL;
                    A_TELEPHONE         : String(30)          NOT NULL;
                    A_DECL_CONFIRM      : String(1)           NOT NULL;
                    C_DECL_DATE         : String(8)           NOT NULL;
                    C_AUTO_APPROVED     : String(1)           NOT NULL;
                    C_LAND_USAGE        : String(1)           NOT NULL;
                    C_FORM_ANSWERS      : String(1)           NOT NULL;
                    C_TMR_ADDRESS       : String(1)           NOT NULL;
                    C_OPEN_TASKS        : String(1)           NOT NULL;
                    Z_RUN_SEQ_ID        : Integer             NOT NULL;
                    Z_CHANGE_TYPE       : String(1)           NOT NULL;
                    Z_CHANGE_TIME       : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
                    Z_SOURCE_SYS        : String(5)           NOT NULL;
                    Z_RUN_ROW_NO        : Integer             NOT NULL;
                    Z_CURR_FLAG         : String(1)           NOT NULL;
                    Z_VALID_FROM        : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
                    Z_VALID_TO          : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1000DCL00000000 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1004LDL00000000
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT           : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID        : String(100)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID        : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID        : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    BUNDLE_ID        : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    VERSION          : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    FORM_NUMBER      : String(4)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    TIMESTAMP        : Decimal(15, 0) NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS    : String(800)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_LIABILITY_YEAR : String(4)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID     : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE    : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME    : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS     : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO     : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG      : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM     : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO       : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000000 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey        : inboundQueueKey;
                    technicalAttributes : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT              : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    BUNDLE_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    VERSION             : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    FORM_NUMBER         : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    TIMESTAMP           : Decimal(15, 0)         NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS       : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_LIABILITY_YEAR    : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME       : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS        : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM        : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO          : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000000 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey        : changeLogKey;
                    technicalAttributes : changeLogAttributes NOT NULL;
                KEY CLIENT              : String(3)           NOT NULL;
                KEY RECORD_ID           : String(100)         NOT NULL;
                    PARENT_ID           : String(32)          NOT NULL;
                    OBJECT_ID           : String(32)          NOT NULL;
                    BUNDLE_ID           : String(32)          NOT NULL;
                    VERSION             : String(5)           NOT NULL;
                    FORM_NUMBER         : String(4)           NOT NULL;
                    TIMESTAMP           : Decimal(15, 0)      NOT NULL;
                    INITIAL_FLAGS       : String(800)         NOT NULL;
                    A_LIABILITY_YEAR    : String(4)           NOT NULL;
                    Z_RUN_SEQ_ID        : Integer             NOT NULL;
                    Z_CHANGE_TYPE       : String(1)           NOT NULL;
                    Z_CHANGE_TIME       : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
                    Z_SOURCE_SYS        : String(5)           NOT NULL;
                    Z_RUN_ROW_NO        : Integer             NOT NULL;
                    Z_CURR_FLAG         : String(1)           NOT NULL;
                    Z_VALID_FROM        : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
                    Z_VALID_TO          : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000000 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1004LDL00000001
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT             : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID          : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID          : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID          : String(32)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS      : String(800)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_EXEMPT_CHECK     : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_PROP_ID          : String(10)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LAND_DESC        : String(25)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LAND_ADDRESS     : String(50)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ENTITLEMENT_LAND : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LAND_USE_CODE    : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LAND_USE_DESC    : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LAND_YEAR        : String(4)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_SCV_ADDRESS      : String(50)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID       : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE      : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME      : UTCTimestamp NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS       : String(5)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO       : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG        : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM       : UTCTimestamp NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO         : UTCTimestamp NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000001 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey        : inboundQueueKey;
                    technicalAttributes : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT              : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS       : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_EXEMPT_CHECK      : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_PROP_ID           : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LAND_DESC         : String(25)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LAND_ADDRESS      : String(50)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ENTITLEMENT_LAND  : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LAND_USE_CODE     : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LAND_USE_DESC     : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LAND_YEAR         : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_SCV_ADDRESS       : String(50)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME       : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS        : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM        : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO          : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000001 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey        : changeLogKey;
                    technicalAttributes : changeLogAttributes NOT NULL;
                KEY CLIENT              : String(3)           NOT NULL;
                KEY RECORD_ID           : String(100)         NOT NULL;
                    PARENT_ID           : String(100)         NOT NULL;
                    OBJECT_ID           : String(32)          NOT NULL;
                    INITIAL_FLAGS       : String(800)         NOT NULL;
                    A_EXEMPT_CHECK      : String(1)           NOT NULL;
                    C_PROP_ID           : String(10)          NOT NULL;
                    C_LAND_DESC         : String(25)          NOT NULL;
                    C_LAND_ADDRESS      : String(50)          NOT NULL;
                    C_ENTITLEMENT_LAND  : String(1)           NOT NULL;
                    C_LAND_USE_CODE     : String(3)           NOT NULL;
                    C_LAND_USE_DESC     : String(40)          NOT NULL;
                    C_LAND_YEAR         : String(4)           NOT NULL;
                    C_SCV_ADDRESS       : String(50)          NOT NULL;
                    Z_RUN_SEQ_ID        : Integer             NOT NULL;
                    Z_CHANGE_TYPE       : String(1)           NOT NULL;
                    Z_CHANGE_TIME       : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
                    Z_SOURCE_SYS        : String(5)           NOT NULL;
                    Z_RUN_ROW_NO        : Integer             NOT NULL;
                    Z_CURR_FLAG         : String(1)           NOT NULL;
                    Z_VALID_FROM        : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
                    Z_VALID_TO          : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 23:59:59';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000001 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1002CDL00000000
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT              : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID           : String(100)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID        : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID           : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID           : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    BUNDLE_ID           : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    VERSION             : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    FORM_NUMBER         : String(4)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    TIMESTAMP           : Decimal(15, 0) NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS       : String(800)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LAST_NAME         : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_FIRST_NAME        : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_MIDDLE_NAME       : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_DATEOFBIRTH       : String(8)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ORG_NAME1         : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ORG_NAME2         : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ACN               : String(20)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ABN               : String(20)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUST_OTHER       : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_APPOINT_MADE      : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_CARE_OF           : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_UNITBLDG          : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_HOUSENO           : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_STREET            : String(60)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_CITY              : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_COUNTRY           : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_REGION            : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_POSTCODE          : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_AD_CHANGED        : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_STREET_POST_IND   : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_CARE_OF       : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_UNITBLDG      : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_HOUSENO       : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_STREET        : String(60)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_POSTAL_NUMBER : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_STREET_TYPE   : String(50)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_POSTAL_TYPE   : String(25)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_CITY          : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_COUNTRY       : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_REGION        : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_POSTCODE      : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_EMAIL_ADDRESS     : String(241)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_COMM_PHONE        : String(30)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_COMM_MOBILE       : String(30)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ADR_GUID          : String(16)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ADR_KIND          : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ADR_NUM           : String(10)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_IS_BP_DETAIL_UPD  : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_COMM_CHANGED      : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_EMAIL_ADDRESS : String(241)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_COMM_PHONE    : String(30)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_UPD_COMM_MOBILE   : String(30)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUST_DOCS        : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUST_TYPE        : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_BPTYPE            : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_ORGTYPE           : String(4)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE       : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME       : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS        : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO        : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG         : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM        : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO          : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000000 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey        : inboundQueueKey;
                    technicalAttributes : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT              : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    BUNDLE_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    VERSION             : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    FORM_NUMBER         : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    TIMESTAMP           : Decimal(15, 0)         NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS       : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LAST_NAME         : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_FIRST_NAME        : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_MIDDLE_NAME       : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_DATEOFBIRTH       : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ORG_NAME1         : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ORG_NAME2         : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ACN               : String(20)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ABN               : String(20)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUST_OTHER       : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_APPOINT_MADE      : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_CARE_OF           : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_UNITBLDG          : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_HOUSENO           : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_STREET            : String(60)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_CITY              : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_COUNTRY           : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_REGION            : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_POSTCODE          : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_AD_CHANGED        : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_STREET_POST_IND   : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_CARE_OF       : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_UNITBLDG      : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_HOUSENO       : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_STREET        : String(60)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_POSTAL_NUMBER : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_STREET_TYPE   : String(50)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_POSTAL_TYPE   : String(25)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_CITY          : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_COUNTRY       : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_REGION        : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_POSTCODE      : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_EMAIL_ADDRESS     : String(241)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_COMM_PHONE        : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_COMM_MOBILE       : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ADR_GUID          : String(16)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ADR_KIND          : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ADR_NUM           : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_IS_BP_DETAIL_UPD  : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_COMM_CHANGED      : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_EMAIL_ADDRESS : String(241)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_COMM_PHONE    : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_UPD_COMM_MOBILE   : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUST_DOCS        : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUST_TYPE        : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_BPTYPE            : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_ORGTYPE           : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME       : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS        : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM        : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO          : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000000 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey        : changeLogKey;
                    technicalAttributes : changeLogAttributes NOT NULL;
                KEY CLIENT              : String(3)           NOT NULL;
                KEY RECORD_ID           : String(100)         NOT NULL;
                    Z_RUN_SEQ_ID        : Integer             NOT NULL;
                    PARENT_ID           : String(32)          NOT NULL;
                    OBJECT_ID           : String(32)          NOT NULL;
                    BUNDLE_ID           : String(32)          NOT NULL;
                    VERSION             : String(5)           NOT NULL;
                    FORM_NUMBER         : String(4)           NOT NULL;
                    TIMESTAMP           : Decimal(15, 0)      NOT NULL;
                    INITIAL_FLAGS       : String(800)         NOT NULL;
                    C_LAST_NAME         : String(40)          NOT NULL;
                    C_FIRST_NAME        : String(40)          NOT NULL;
                    C_MIDDLE_NAME       : String(40)          NOT NULL;
                    A_DATEOFBIRTH       : String(8)           NOT NULL;
                    C_ORG_NAME1         : String(40)          NOT NULL;
                    C_ORG_NAME2         : String(40)          NOT NULL;
                    C_ACN               : String(20)          NOT NULL;
                    C_ABN               : String(20)          NOT NULL;
                    A_TRUST_OTHER       : String(40)          NOT NULL;
                    A_APPOINT_MADE      : String(1)           NOT NULL;
                    C_CARE_OF           : String(40)          NOT NULL;
                    C_UNITBLDG          : String(40)          NOT NULL;
                    C_HOUSENO           : String(10)          NOT NULL;
                    C_STREET            : String(60)          NOT NULL;
                    C_CITY              : String(40)          NOT NULL;
                    C_COUNTRY           : String(3)           NOT NULL;
                    C_REGION            : String(3)           NOT NULL;
                    C_POSTCODE          : String(10)          NOT NULL;
                    A_AD_CHANGED        : String(1)           NOT NULL;
                    A_STREET_POST_IND   : String(1)           NOT NULL;
                    A_UPD_CARE_OF       : String(40)          NOT NULL;
                    A_UPD_UNITBLDG      : String(40)          NOT NULL;
                    A_UPD_HOUSENO       : String(10)          NOT NULL;
                    A_UPD_STREET        : String(60)          NOT NULL;
                    A_UPD_POSTAL_NUMBER : String(10)          NOT NULL;
                    A_UPD_STREET_TYPE   : String(50)          NOT NULL;
                    A_UPD_POSTAL_TYPE   : String(25)          NOT NULL;
                    A_UPD_CITY          : String(40)          NOT NULL;
                    A_UPD_COUNTRY       : String(3)           NOT NULL;
                    A_UPD_REGION        : String(3)           NOT NULL;
                    A_UPD_POSTCODE      : String(10)          NOT NULL;
                    C_EMAIL_ADDRESS     : String(241)         NOT NULL;
                    C_COMM_PHONE        : String(30)          NOT NULL;
                    C_COMM_MOBILE       : String(30)          NOT NULL;
                    C_ADR_GUID          : String(16)          NOT NULL;
                    C_ADR_KIND          : String(10)          NOT NULL;
                    C_ADR_NUM           : String(10)          NOT NULL;
                    C_IS_BP_DETAIL_UPD  : String(1)           NOT NULL;
                    A_COMM_CHANGED      : String(1)           NOT NULL;
                    A_UPD_EMAIL_ADDRESS : String(241)         NOT NULL;
                    A_UPD_COMM_PHONE    : String(30)          NOT NULL;
                    A_UPD_COMM_MOBILE   : String(30)          NOT NULL;
                    A_TRUST_DOCS        : String(1)           NOT NULL;
                    A_TRUST_TYPE        : String(3)           NOT NULL;
                    C_BPTYPE            : String(1)           NOT NULL;
                    C_ORGTYPE           : String(4)           NOT NULL;
                    Z_CHANGE_TYPE       : String(1)           NOT NULL;
                    Z_CHANGE_TIME       : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_SOURCE_SYS        : String(5)           NOT NULL;
                    Z_RUN_ROW_NO        : Integer             NOT NULL;
                    Z_CURR_FLAG         : String(1)           NOT NULL;
                    Z_VALID_FROM        : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_VALID_TO          : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000000 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1002CDL00000001
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT             : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID          : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID       : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID          : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID          : String(32)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS      : String(800)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_LAST_NAME   : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_FIRST_NAME  : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_MIDDLE_NAME : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_DATEOFBIRTH : String(8)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_ORG_NAME    : String(80)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_ACN         : String(20)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_ABN         : String(20)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_CARE_OF     : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_UNITBLDG    : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_HOUSENO     : String(10)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_STREET      : String(60)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_CITY        : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_COUNTRY     : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_REGION      : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_POSTCODE    : String(10)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_PHONE       : String(30)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_EMAIL_ADD   : String(241)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_BENE_TYPE        : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE      : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME      : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS       : String(5)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO       : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG        : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM       : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO         : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000001 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey        : inboundQueueKey;
                    technicalAttributes : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT              : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS       : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_LAST_NAME    : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_FIRST_NAME   : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_MIDDLE_NAME  : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_DATEOFBIRTH  : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_ORG_NAME     : String(80)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_ACN          : String(20)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_ABN          : String(20)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_CARE_OF      : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_UNITBLDG     : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_HOUSENO      : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_STREET       : String(60)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_CITY         : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_COUNTRY      : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_REGION       : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_POSTCODE     : String(10)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_PHONE        : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_EMAIL_ADD    : String(241)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_BENE_TYPE         : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME       : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS        : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM        : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO          : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000001 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey        : changeLogKey;
                    technicalAttributes : changeLogAttributes NOT NULL;
                KEY CLIENT              : String(3)           NOT NULL;
                KEY RECORD_ID           : String(100)         NOT NULL;
                    Z_RUN_SEQ_ID        : Integer             NOT NULL;
                    PARENT_ID           : String(100)         NOT NULL;
                    OBJECT_ID           : String(32)          NOT NULL;
                    INITIAL_FLAGS       : String(800)         NOT NULL;
                    A_BENE_LAST_NAME    : String(40)          NOT NULL;
                    A_BENE_FIRST_NAME   : String(40)          NOT NULL;
                    A_BENE_MIDDLE_NAME  : String(40)          NOT NULL;
                    A_BENE_DATEOFBIRTH  : String(8)           NOT NULL;
                    A_BENE_ORG_NAME     : String(80)          NOT NULL;
                    A_BENE_ACN          : String(20)          NOT NULL;
                    A_BENE_ABN          : String(20)          NOT NULL;
                    A_BENE_CARE_OF      : String(40)          NOT NULL;
                    A_BENE_UNITBLDG     : String(40)          NOT NULL;
                    A_BENE_HOUSENO      : String(10)          NOT NULL;
                    A_BENE_STREET       : String(60)          NOT NULL;
                    A_BENE_CITY         : String(40)          NOT NULL;
                    A_BENE_COUNTRY      : String(3)           NOT NULL;
                    A_BENE_REGION       : String(3)           NOT NULL;
                    A_BENE_POSTCODE     : String(10)          NOT NULL;
                    A_BENE_PHONE        : String(30)          NOT NULL;
                    A_BENE_EMAIL_ADD    : String(241)         NOT NULL;
                    A_BENE_TYPE         : String(3)           NOT NULL;
                    Z_CHANGE_TYPE       : String(1)           NOT NULL;
                    Z_CHANGE_TIME       : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_SOURCE_SYS        : String(5)           NOT NULL;
                    Z_RUN_ROW_NO        : Integer             NOT NULL;
                    Z_CURR_FLAG         : String(1)           NOT NULL;
                    Z_VALID_FROM        : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_VALID_TO          : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000001 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1002CDL00000002
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT                : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID             : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID          : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID             : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID             : String(32)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS         : String(800)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_TYPE        : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_LAST_NAME   : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_FIRST_NAME  : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_MIDDLE_NAME : String(40)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_DATEOFBIRTH : String(8)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_ORG_NAME    : String(80)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_ACN         : String(20)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_TRUSTEE_ABN         : String(20)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE         : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME         : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS          : String(5)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO          : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG           : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM          : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO            : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000002 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey          : inboundQueueKey;
                    technicalAttributes   : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT                : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID             : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID          : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID             : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID             : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS         : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_TYPE        : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_LAST_NAME   : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_FIRST_NAME  : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_MIDDLE_NAME : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_DATEOFBIRTH : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_ORG_NAME    : String(80)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_ACN         : String(20)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_TRUSTEE_ABN         : String(20)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME         : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS          : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO          : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG           : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM          : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO            : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000002 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey          : changeLogKey;
                    technicalAttributes   : changeLogAttributes NOT NULL;
                KEY CLIENT                : String(3)           NOT NULL;
                KEY RECORD_ID             : String(100)         NOT NULL;
                    Z_RUN_SEQ_ID          : Integer             NOT NULL;
                    PARENT_ID             : String(100)         NOT NULL;
                    OBJECT_ID             : String(32)          NOT NULL;
                    INITIAL_FLAGS         : String(800)         NOT NULL;
                    A_TRUSTEE_TYPE        : String(3)           NOT NULL;
                    A_TRUSTEE_LAST_NAME   : String(40)          NOT NULL;
                    A_TRUSTEE_FIRST_NAME  : String(40)          NOT NULL;
                    A_TRUSTEE_MIDDLE_NAME : String(40)          NOT NULL;
                    A_TRUSTEE_DATEOFBIRTH : String(8)           NOT NULL;
                    A_TRUSTEE_ORG_NAME    : String(80)          NOT NULL;
                    A_TRUSTEE_ACN         : String(20)          NOT NULL;
                    A_TRUSTEE_ABN         : String(20)          NOT NULL;
                    Z_CHANGE_TYPE         : String(1)           NOT NULL;
                    Z_CHANGE_TIME         : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_SOURCE_SYS          : String(5)           NOT NULL;
                    Z_RUN_ROW_NO          : Integer             NOT NULL;
                    Z_CURR_FLAG           : String(1)           NOT NULL;
                    Z_VALID_FROM          : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_VALID_TO            : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1002CDL00000002 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1004LDL00000002
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT              : String(3)    NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID           : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID        : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID           : String(100)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID           : String(32)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS       : String(800)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_MAT_ADDRESS       : String(255)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_LICENSE_NO        : String(15)   NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_TMR_LAST_MOD_DATE : String(8)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE       : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME       : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS        : String(5)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO        : Integer      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG         : String(1)    NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM        : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO          : UTCTimestamp NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000002 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey        : inboundQueueKey;
                    technicalAttributes : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT              : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID           : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID           : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS       : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_MAT_ADDRESS       : String(255)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_LICENSE_NO        : String(15)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_TMR_LAST_MOD_DATE : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME       : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS        : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO        : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM        : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO          : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000002 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey        : changeLogKey;
                    technicalAttributes : changeLogAttributes NOT NULL;
                KEY CLIENT              : String(3)           NOT NULL;
                KEY RECORD_ID           : String(100)         NOT NULL;
                    Z_RUN_SEQ_ID        : Integer             NOT NULL;
                    PARENT_ID           : String(100)         NOT NULL;
                    OBJECT_ID           : String(32)          NOT NULL;
                    INITIAL_FLAGS       : String(800)         NOT NULL;
                    C_MAT_ADDRESS       : String(255)         NOT NULL;
                    C_LICENSE_NO        : String(15)          NOT NULL;
                    C_TMR_LAST_MOD_DATE : String(8)           NOT NULL;
                    Z_CHANGE_TYPE       : String(1)           NOT NULL;
                    Z_CHANGE_TIME       : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_SOURCE_SYS        : String(5)           NOT NULL;
                    Z_RUN_ROW_NO        : Integer             NOT NULL;
                    Z_CURR_FLAG         : String(1)           NOT NULL;
                    Z_VALID_FROM        : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_VALID_TO          : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1004LDL00000002 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1006LUL00000000
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT               : String(3)       NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID            : String(100)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID         : Integer         NOT NULL;

                    @DataStore.aggregation : #NOP
                    PARENT_ID            : String(32)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID            : String(32)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    BUNDLE_ID            : String(32)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    VERSION              : String(5)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    FORM_NUMBER          : String(4)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    TIMESTAMP            : Decimal(15, 0)  NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS        : String(800)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_1A           : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_COMM_OCC_DATE      : String(8)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_1B_TEXTBOX   : String(255)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_1C           : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_1C_ONE       : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_1C_TWO       : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_1C_THREE     : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_CEASE_RES_DATE     : String(8)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REOCCUP_DATE       : String(8)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_1C_TEXTBOX   : String(255)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_2A           : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_2A_TEXTBOX   : String(255)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_2B           : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_2B_TEXTBOX   : String(255)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_3A           : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_QUEST_3A_TEXTBOX   : String(255)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_3B_PROP_PERCENTAGE : Decimal(16, 12) NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE        : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME        : UTCTimestamp    NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS         : String(5)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO         : Integer         NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG          : String(1)       NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM         : UTCTimestamp    NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO           : UTCTimestamp    NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1006LUL00000000 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey         : inboundQueueKey;
                    technicalAttributes  : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT               : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID            : String(100)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_SEQ_ID         : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    PARENT_ID            : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID            : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    BUNDLE_ID            : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    VERSION              : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    FORM_NUMBER          : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    TIMESTAMP            : Decimal(15, 0)         NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS        : String(800)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_1A           : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_COMM_OCC_DATE      : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_1B_TEXTBOX   : String(255)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_1C           : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_1C_ONE       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_1C_TWO       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_1C_THREE     : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_CEASE_RES_DATE     : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REOCCUP_DATE       : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_1C_TEXTBOX   : String(255)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_2A           : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_2A_TEXTBOX   : String(255)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_2B           : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_2B_TEXTBOX   : String(255)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_3A           : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_QUEST_3A_TEXTBOX   : String(255)            NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_3B_PROP_PERCENTAGE : Decimal(16, 12)        NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE        : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME        : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS         : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO         : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG          : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM         : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO           : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1006LUL00000000 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey         : changeLogKey;
                    technicalAttributes  : changeLogAttributes NOT NULL;
                KEY CLIENT               : String(3)           NOT NULL;
                KEY RECORD_ID            : String(100)         NOT NULL;
                    Z_RUN_SEQ_ID         : Integer             NOT NULL;
                    PARENT_ID            : String(32)          NOT NULL;
                    OBJECT_ID            : String(32)          NOT NULL;
                    BUNDLE_ID            : String(32)          NOT NULL;
                    VERSION              : String(5)           NOT NULL;
                    FORM_NUMBER          : String(4)           NOT NULL;
                    TIMESTAMP            : Decimal(15, 0)      NOT NULL;
                    INITIAL_FLAGS        : String(800)         NOT NULL;
                    A_QUEST_1A           : String(1)           NOT NULL;
                    A_COMM_OCC_DATE      : String(8)           NOT NULL;
                    A_QUEST_1B_TEXTBOX   : String(255)         NOT NULL;
                    A_QUEST_1C           : String(1)           NOT NULL;
                    A_QUEST_1C_ONE       : String(1)           NOT NULL;
                    A_QUEST_1C_TWO       : String(1)           NOT NULL;
                    A_QUEST_1C_THREE     : String(1)           NOT NULL;
                    A_CEASE_RES_DATE     : String(8)           NOT NULL;
                    A_REOCCUP_DATE       : String(8)           NOT NULL;
                    A_QUEST_1C_TEXTBOX   : String(255)         NOT NULL;
                    A_QUEST_2A           : String(1)           NOT NULL;
                    A_QUEST_2A_TEXTBOX   : String(255)         NOT NULL;
                    A_QUEST_2B           : String(1)           NOT NULL;
                    A_QUEST_2B_TEXTBOX   : String(255)         NOT NULL;
                    A_QUEST_3A           : String(1)           NOT NULL;
                    A_QUEST_3A_TEXTBOX   : String(255)         NOT NULL;
                    A_3B_PROP_PERCENTAGE : Decimal(16, 12)     NOT NULL;
                    Z_CHANGE_TYPE        : String(1)           NOT NULL;
                    Z_CHANGE_TIME        : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_SOURCE_SYS         : String(5)           NOT NULL;
                    Z_RUN_ROW_NO         : Integer             NOT NULL;
                    Z_CURR_FLAG          : String(1)           NOT NULL;
                    Z_VALID_FROM         : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_VALID_TO           : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1006LUL00000000 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };

        @DataStore.isDSO
        @DataStore.entity.activeData         : 'active_data'
        @DataStore.entity.changeLog          : 'change_log'
        @DataStore.entity.activationQueue    : [ 'inbound_queue' ]
        @DataStore.entity.operationHistory   : 'operationHistory'
        @DataStore.entity.aggregationHistory : 'aggregationHistory'
        @DataStore.entity.logMessages        : 'logMessages'
        @DataStore.entity.affectedRequests   : 'affectedRequests'
        @DataStore.entity.subscribers        : 'subscribers'
        @DataStore.entity.runningOperations  : 'runningOperations'
        @DataStore.entity.idGenerator        : 'idGen'
        @DataStore.computeChecksum           : false
        @DataStore.snapshotSupport           : false
        CONTEXT Z1009DCR00000000
        {
            @DataStore.entityCategory : #ACTIVE_DATA
            ENTITY active_data
            {
                    @DataStore.aggregation : #NOP
                KEY CLIENT                    : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                KEY RECORD_ID                 : String(500)    NOT NULL DEFAULT '';

                    @DataStore.aggregation : #NOP
                    PARENT_ID                 : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    OBJECT_ID                 : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    BUNDLE_ID                 : String(32)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    VERSION                   : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    FORM_NUMBER               : String(4)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    TIMESTAMP                 : Decimal(15, 0) NOT NULL;

                    @DataStore.aggregation : #NOP
                    INITIAL_FLAGS             : String(500)    NOT NULL DEFAULT '';

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_EXTACCT      : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_STAFF        : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_GRPMEM       : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_OSR          : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_EXTAUDIT     : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_WEBINAR      : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_OTHER        : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_REG_OTHER_TXT    : String(30)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_VER_NAME                : String(40)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_VER_CONTACT_PHONE       : String(30)     NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_VER_CONFIRM             : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_VER_DATE                : String(8)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_REV_PT_ICR_IND          : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    A_REV_PT_ICR_FREQ         : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_9DCR_HAPPY_PATH         : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    C_9DCR_NOT_HAPPY_PATH_RSN : String(3)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TYPE             : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CHANGE_TIME             : UTCTimestamp   NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #NOP
                    Z_SOURCE_SYS              : String(5)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_ROW_NO              : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_RUN_SEQ_ID              : Integer        NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_CURR_FLAG               : String(1)      NOT NULL;

                    @DataStore.aggregation : #NOP
                    Z_VALID_FROM              : LocalDate      NOT NULL DEFAULT '1900-01-01';

                    @DataStore.aggregation : #NOP
                    Z_VALID_TO                : LocalDate      NOT NULL DEFAULT '1900-01-01';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1009DCR00000000 group type "sap.dwf.dso" group subtype ACTIVE;
            };

            @DataStore.entityCategory : #ACTIVATION_QUEUE
            ENTITY inbound_queue
            {
                KEY technicalKey              : inboundQueueKey;
                    technicalAttributes       : inboundQueueAttributes NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY CLIENT                    : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY RECORD_ID                 : String(500)            NOT NULL DEFAULT '';

                    @DataStore.aggregation : #MOV
                    PARENT_ID                 : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    OBJECT_ID                 : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    BUNDLE_ID                 : String(32)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    VERSION                   : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    FORM_NUMBER               : String(4)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    TIMESTAMP                 : Decimal(15, 0)         NOT NULL;

                    @DataStore.aggregation : #MOV
                    INITIAL_FLAGS             : String(500)            NOT NULL DEFAULT '';

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_EXTACCT      : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_STAFF        : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_GRPMEM       : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_OSR          : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_EXTAUDIT     : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_WEBINAR      : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_OTHER        : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_REG_OTHER_TXT    : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_VER_NAME                : String(40)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_VER_CONTACT_PHONE       : String(30)             NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_VER_CONFIRM             : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_VER_DATE                : String(8)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_REV_PT_ICR_IND          : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    A_REV_PT_ICR_FREQ         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_9DCR_HAPPY_PATH         : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    C_9DCR_NOT_HAPPY_PATH_RSN : String(3)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TYPE             : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CHANGE_TIME             : UTCTimestamp           NOT NULL DEFAULT '1900-01-01 00:00:00';

                    @DataStore.aggregation : #MOV
                    Z_SOURCE_SYS              : String(5)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_RUN_ROW_NO              : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                KEY Z_RUN_SEQ_ID              : Integer                NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_CURR_FLAG               : String(1)              NOT NULL;

                    @DataStore.aggregation : #MOV
                    Z_VALID_FROM              : LocalDate              NOT NULL DEFAULT '1900-01-01';

                    @DataStore.aggregation : #MOV
                    Z_VALID_TO                : LocalDate              NOT NULL DEFAULT '1900-01-01';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1009DCR00000000 group type "sap.dwf.dso" group subtype QUEUE;
            };

            @DataStore.entityCategory : #CHANGE_LOG
            ENTITY change_log
            {
                KEY technicalKey              : changeLogKey;
                    technicalAttributes       : changeLogAttributes NOT NULL;
                KEY CLIENT                    : String(3)           NOT NULL;
                KEY RECORD_ID                 : String(500)         NOT NULL DEFAULT '';
                    PARENT_ID                 : String(32)          NOT NULL;
                    OBJECT_ID                 : String(32)          NOT NULL;
                    BUNDLE_ID                 : String(32)          NOT NULL;
                    VERSION                   : String(5)           NOT NULL;
                    FORM_NUMBER               : String(4)           NOT NULL;
                    TIMESTAMP                 : Decimal(15, 0)      NOT NULL;
                    INITIAL_FLAGS             : String(500)         NOT NULL DEFAULT '';
                    A_REV_PT_REG_EXTACCT      : String(1)           NOT NULL;
                    A_REV_PT_REG_STAFF        : String(1)           NOT NULL;
                    A_REV_PT_REG_GRPMEM       : String(1)           NOT NULL;
                    A_REV_PT_REG_OSR          : String(1)           NOT NULL;
                    A_REV_PT_REG_EXTAUDIT     : String(1)           NOT NULL;
                    A_REV_PT_REG_WEBINAR      : String(1)           NOT NULL;
                    A_REV_PT_REG_OTHER        : String(1)           NOT NULL;
                    A_REV_PT_REG_OTHER_TXT    : String(30)          NOT NULL;
                    A_VER_NAME                : String(40)          NOT NULL;
                    A_VER_CONTACT_PHONE       : String(30)          NOT NULL;
                    A_VER_CONFIRM             : String(1)           NOT NULL;
                    C_VER_DATE                : String(8)           NOT NULL;
                    C_REV_PT_ICR_IND          : String(1)           NOT NULL;
                    A_REV_PT_ICR_FREQ         : String(1)           NOT NULL;
                    C_9DCR_HAPPY_PATH         : String(1)           NOT NULL;
                    C_9DCR_NOT_HAPPY_PATH_RSN : String(3)           NOT NULL;
                    Z_CHANGE_TYPE             : String(1)           NOT NULL;
                    Z_CHANGE_TIME             : UTCTimestamp        NOT NULL DEFAULT '1900-01-01 00:00:00';
                    Z_SOURCE_SYS              : String(5)           NOT NULL;
                    Z_RUN_ROW_NO              : Integer             NOT NULL;
                KEY Z_RUN_SEQ_ID              : Integer             NOT NULL;
                    Z_CURR_FLAG               : String(1)           NOT NULL;
                    Z_VALID_FROM              : LocalDate           NOT NULL DEFAULT '1900-01-01';
                    Z_VALID_TO                : LocalDate           NOT NULL DEFAULT '1900-01-01';
            }
            TECHNICAL CONFIGURATION
            {
                group name Z1009DCR00000000 group type "sap.dwf.dso" group subtype CHANGE_LOG;
            };

            @DataStore.entityCategory : #OPERATION_HISTORY
            ENTITY operationHistory
            {
                KEY technicalKey        : operationHistoryKey;
                    technicalAttributes : operationHistoryAttributes;
            };

            @DataStore.entityCategory : #AGGREGATION_HISTORY
            ENTITY aggregationHistory
            {
                KEY technicalKey        : aggregationHistoryKey;
                    technicalAttributes : aggregationHistoryAttributes;
            };

            @DataStore.entityCategory : #AFFECTED_REQUESTS
            ENTITY affectedRequests
            {
                KEY technicalKey : affectedRequestsKey;
            };

            @DataStore.entityCategory : #LOG_MESSAGES
            ENTITY logMessages
            {
                KEY technicalKey        : logMessagesKey;
                    technicalAttributes : logMessagesAttributes;
            };

            @DataStore.entityCategory : #SUBSCRIBERS
            ENTITY subscribers
            {
                KEY technicalKey        : subscribersKey;
                    technicalAttributes : subscribersAttributes;
            };

            @DataStore.entityCategory : #RUNNING_OPERATIONS
            ENTITY runningOperations
            {
                KEY technicalKey : runningOperationsKey;
            };

            @DataStore.entityCategory : #ID_GENERATOR
            ENTITY idGen
            {
                KEY technicalKey        : idGeneratorKey;
                    technicalAttributes : idGeneratorAttributes;
            };
        };
    };
};